export default class Index extends Component {
  render() {
    return null;
  }
}

// merge-h5-config.input.config.js
/*
let config = {
  navigationBarTitleText: '默认'
};

if (process.env.TARO_ENV === 'h5') {
  config = {
    navigationBarTitleText: '首页'
  };
}

export default config;
*/
