export default class Index extends Component {
  render() {
    return null;
  }
}

// h5-config.input.config.js
/*
let config;

if (process.env.TARO_ENV === 'h5') {
    config = {
      navigationBarTitleText: '首页'
    };
}

export default config;
*/
